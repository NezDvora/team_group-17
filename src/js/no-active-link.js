const link = document.getElementById('my-link');
link.addEventListener('click', function (event) {
  event.preventDefault(); // Предотвращаем обновление страницы
  // Другой код, который вы хотите выполнить при клике на ссылку
});